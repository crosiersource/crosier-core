<div class="card">
    <div class="card-header">

        <div class="row">
            <div class="col-8">
                <h3></h3>
            </div>
            <div class="col-4 text-right">
                <button type="button" class="btn btn-primary"
                        onclick="window.location.href='{{ url(route_form, {ref: ref.id}) }}'">
                    <i class="fas fa-file" aria-hidden="true"></i> Novo
                </button>
            </div>
        </div>
    </div>

    <div class="card-body">
        <div class="table-responsive-sm">

            <table class="table table-striped table-hover display compact">
                <thead>
                <tr>
                    <th scope="col">Endereço</th>
                    <th scope="col">Tipo</th>
                </tr>
                </thead>

                <tbody>
                {% for item in ref.enderecos %}
                    <tr>
                        <td>{{ item.enderecoCompleto }}<br/>{{ item.cidade }}-{{ item.estado }}</td>
                        <td>{{ item.tipoEndereco }}</td>
                        <td class="text-right">
                            <button type="button" class="btn btn-primary"
                                    onclick="window.location.href='{{ url(route_form, {ref: ref.id, endereco: item.id}) }}'">
                                <i class="fas fa-wrench" aria-hidden="true"></i>
                            </button>

                            <button type="button" class="btn btn-danger"
                                    data-url="{{ url(route_delete, {ref: ref.id, endereco: item.id}) }}"
                                    data-token="{{ csrf_token('delete') }}"
                                    data-target="#confirmationModal" data-toggle="modal">
                                <i class="fa fa-trash" aria-hidden="true"></i>
                            </button>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>

        </div>
    </div>
</div>